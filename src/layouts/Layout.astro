<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		
		<link rel="icon" type="image/png" sizes="512x512" href="/ramo.png?v=2" />
		<link rel="shortcut icon" type="image/x-icon" href="/ramo.ico?v=2" />
		
		<meta name="generator" content={Astro.generator} />
		
		<title>Victor & Jhenifer | Invitación</title>
	</head>

	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		--grosor-fuente-light: 300;
		--grosor-fuente-regular: 400;
		--grosor-fuente-semi-bold: 600;
		--grosor-fuente-bold: 700;

		--fuente-1: Georgia, 'Times New Roman', serif;
		--fuente-2: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

		--font-titulos: var(--fuente-1);
		--grosor-titulos: var(--grosor-fuente-regular);

		--font-subtitulos: var(--fuente-2);
		--grosor-subtitulos: var(--grosor-fuente-light);

		--font-nombres-portada: var(--fuente-1);
		--grosor-nombres-portada: var(--grosor-fuente-regular);

		--font-frase-portada: var(--fuente-2);
		--grosor-frase-portada: var(--grosor-fuente-light);

		--font-nombres-footer: var(--fuente-1);
		--grosor-nombres-footer: var(--grosor-fuente-regular);

		--color-pallete-1: #376282;
		--color-pallete-2: #04294A;
		--color-pallete-3: #FFFFFF;
		--color-pallete-4: #DAE8F1;
		--color-pallete-5: #EEF8FF;

		--color-fondo-body: var(--color-pallete-1);
		--color-sombra-container-body: var(--color-pallete-2);
		--color-titulos: var(--color-pallete-1);
		--color-subtitulos: var(--color-pallete-2);
		--color-circulos-anim-reproductor: var(--color-pallete-1);
		--color-nota-anim-reproductor: var(--color-pallete-1);
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: var(--fuente-2);
		font-size: 1rem;
		font-weight: var(--grosor-fuente-regular);
		line-height: 1.5;
		color: #01203f;
		background-color: var(--color-fondo-body);
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		overflow-x: hidden;
	}

	@media (prefers-reduced-motion: reduce) {
		html {
			scroll-behavior: auto;
		}
	}
</style>